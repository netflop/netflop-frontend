export const RESPONSE_CODE: Record<string, number> = {
  SUCCESS: 200,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401
};

export const AUTO_FOCUS_INPUT_DELAY = 300;
export const localStorageKeys = {
  IS_DARK_MODE: 'isDarkMode'
};
